# CAG cache (8B.1 scaffold)

Archivo generado:

```txt
docs/knowledge/cache/eva-cache-v1.json
```

Regenerar:

```bash
node scripts/build-eva-cag-cache.mjs
```

No editar el JSON a mano. Invalidación: cambiar `static/*.md` o `metadata.json` y rebuild.
